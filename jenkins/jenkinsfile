pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'pip --version'
                sh 'make makefile lint'
                sh 'make makefile docs'
            }
        }

    }
}
