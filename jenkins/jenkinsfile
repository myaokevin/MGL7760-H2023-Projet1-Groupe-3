pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'pip --version'
                sh 'make makefile docs'
                sh 'make makefile all'
            }
        }

    }
}
