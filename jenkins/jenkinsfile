pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh 'pip --version'
            }
        }
        stage('build') {
            steps {
                sh 'make makefile lint'
            }
        }
    }
}
