pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'pip --version'
            }
        }
        stage('analyse statique') {
            steps {
                sh 'make makefile lint'
            }
        }
    }
}
