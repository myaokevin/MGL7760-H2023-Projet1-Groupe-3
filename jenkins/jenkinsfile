pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'whereis pylint'
            }
        }
        stage('analyse statique') {
            steps {
                sh 'make makefile lint'
            }
        }
    }
}
